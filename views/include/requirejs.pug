//- Google Analytics
script
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-84076310-1', 'auto', {'allowLinker': true});
    ga('require', 'linker');
    ga('linker:autoLink', ['mac.iupui.edu']);
    ga('send', 'pageview');

script(src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.4/jquery.min.js")
script(src="https://cdnjs.cloudflare.com/ajax/libs/require.js/2.1.22/require.min.js")
script(src="/js/common.js")
-
    var bootstrap = "\"bootstrap\", ";
    if (noExternalCSS) {
        bootstrap = "";
    }
    var extraJSFinal = "";
    if (extraJS) {
        extraJSFinal = ",'" + extraJS.join(",").replace(/\,/g, "','") + "'";
    }
script.
    require(["jquery", "jquery-ui", "es6-shim"!{extraJSFinal}], function() {
        require([!{bootstrap}"#{mainjs}"]);
    });
